class FilmParse {
    constructor(name) {
        this.title = title;

    }

    static parse(format) {
        let films = [];
        for (let film of format) {
            films.push (
               film.title

            )
        }
        return films;

    }

}

module.exports = FilmParse;